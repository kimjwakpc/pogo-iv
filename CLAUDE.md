# 포켓몬고 개체값 판독기 — 작업 지침

## 이 폴더

`index.html` 하나가 앱 전체입니다. 빌드 과정 없이 브라우저로 바로 열립니다.
GitHub Pages로 배포해서 본인과 친구 몇 명이 사용합니다.

## 대화 원칙

- **존댓말**을 씁니다. 반말은 쓰지 않습니다.
- 추천이나 방향을 묻는 질문에는 **되묻지 말고 바로 결론**을 제시합니다.
- 포켓몬 한글 이름은 **추측하지 말고 반드시 데이터로 확인**합니다. 과거에 소곤룡·탱그릴·나오하·냐오불을 잘못 짚어 잘못된 판정을 드린 적이 있습니다.

## 수정할 때 반드시 지킬 것

1. **`node test.mjs` 를 돌립니다.** 문법 검사와 아래 기대값 대조를 한 번에 합니다. 통과하지 못한 채로 두지 않습니다.

```
node test.mjs             # 평소 (.pvcache 가 있으면 그걸 씀)
node test.mjs --refresh   # pvpoke 데이터를 새로 받아 캐시 갱신
```

   레이드 검사는 저장소의 `pve.json` 을 그대로 읽습니다. 없으면 `node tools/build-pve.mjs` 를 먼저 돌리세요.

   `test.mjs` 는 `<script>` 블록을 뽑아 `new Function` 으로 문법을 보고, 가짜 DOM·localStorage·fetch 를 씌워 실제로 실행한 뒤 아래 표와 대조합니다. 한글 이름 오인 사례 12건의 진화 계통도 함께 확인합니다. pvpoke 데이터는 `.pvcache/` 에 받아 둡니다(git 에 넣지 않습니다).

2. **계산부는 검증된 값과 대조합니다.** 아래 값이 그대로 나와야 합니다.

| 대상 | 리그 | 기대값 |
|---|---|---|
| 따라큐 1/14/15 | 슈퍼 | 랭크1, Lv25.5, CP1500 |
| 갸라도스 0/14/15 | 슈퍼 | 랭크1 100%, Lv16.5, CP1499 |
| 갸라도스 0/15/14 | 하이퍼 | 랭크1 100%, Lv27.5, CP2500 |
| 요가램 5/15/15 | 슈퍼 | 랭크1, Lv50 |
| 전룡 0/13/15 | 하이퍼 | 랭크1 100%, Lv36 |
| 아머까오 2/15/15 | 하이퍼 | 10위 99.5%, **Lv47.5**, CP2499 |
| XL사탕 | Lv40→50 | 296개 |

3. **데이터를 임의로 하드코딩하지 않습니다.** 순위·기술은 실행 시 pvpoke에서 받아옵니다.

## 데이터 기준일 표시

머리말 오른쪽에 `gamemaster.json` 의 `timestamp`(pvpoke 자료 생성 시점)를 띄웁니다.
밸런스 패치일이 지났는데 자료가 그보다 오래되면 화면 위에 노란 알림과 "다시 받기" 버튼이 뜹니다.
새 패치가 생기면 스크립트의 `PATCHES` 배열에 `['YYYY-MM-DD','패치 이름']` 한 줄만 더하면 됩니다.

## 구조 요약

- **데이터 출처(런타임 fetch)**: `raw.githubusercontent.com/pvpoke/pvpoke/master/src/data/`
  - `gamemaster.json` — 종족값, 기술풀, `eliteMoves`(대기머), `family`(진화)
  - `rankings/all/overall/rankings-1500.json`, `rankings-2500.json`
- **임베드 데이터**: `KO_NAME`(도감번호→한글명 1025종), `KO_MOVE`(기술 한글명 300개), `FORM_KO`(폼 한글명), `NOT_FORM`(괄호가 이름의 일부인 것), `FORM_KEEP`(원문 유지), `TYPE_KO`/`TYPE_C`

## 폼 한글명 (`FORM_KO`)

- 표기는 PokeAPI `pokemon_form_names.csv` 의 한국어(`language_id=3`)를 따릅니다. 주석에 `※` 가 붙은 항목은 공식 표기를 확인하지 못해 통용 표기를 쓴 것입니다.
- 같은 토큰이 포켓몬마다 뜻이 다르면 `"도감번호|토큰"` 키로 덮어씁니다. 예: `"875|Ice":"아이스페이스"` (빙큐보는 얼음 타입폼이 아님).
- `Type (Null)` · `Mime (Jr)` 처럼 괄호가 이름의 일부인 것은 `NOT_FORM` 에 넣어 건너뜁니다.
- 포켓몬고 전용 코스튬 피카츄는 공식 한국어 표기를 확인하지 못해 `FORM_KEEP` 에 넣고 원문을 그대로 둡니다.
- `test.mjs` 가 **매핑 안 된 폼 토큰이 하나라도 있으면 실패**시킵니다. pvpoke 에 새 폼이 들어오면 여기서 걸립니다.
- **계산**: 내장 CPM 테이블 → `maxLevel()` 이분탐색 → 4096 IV 전수 → 스탯곱 정렬
- **저장**: localStorage (`ivx_box` 보관함, `ivx_names` 이름수정, `ivx_cfg` 판정기준, `ivx_alias` 별명)

## 검색

- **도감번호 · 한글명 · 영문명 · speciesId · 진화 전 이름** 으로 찾습니다.
- **초성 검색**: `ㄱㄹㄷㅅ` → 갸라도스. 질문이 전부 초성(`ㄱ`~`ㅎ`)일 때만 초성 모드로 들어갑니다.
- **별명**: `ALIAS`(별명 → speciesId, localStorage `ivx_alias`). 정확히 일치하면 그 한 마리만 내놓고, 부분 일치는 보통 검색 결과에 섞습니다. 판정 화면의 "별명" 버튼으로 등록하고 설정 탭에서 관리·내보내기 합니다.
- 정렬은 **딱 맞음 → 앞부분 맞음 → 나머지**, 같은 등급이면 기본형 → 메가 → 그림자 순입니다.

## 이 폴더에서 git 을 다룰 때

**Claude 는 이 폴더에서 `git` 명령을 실행하지 않습니다.** 원격 셸에 파일 삭제 권한이 없어서
`git status` 조차 `.git/index.lock` 을 남기고, 그러면 GitHub Desktop 이 막힙니다.
커밋·푸시는 사용자가 GitHub Desktop 에서 합니다. 상태를 봐야 하면 사용자에게 물어보세요.

## 레이드 탭

레이드는 개체값 100짜리를 쓰는 게 정답이라 개체 판독이 의미가 없습니다.
그래서 개체값을 받지 않고 **타입별 순위 · 기술배치 · 대기머 여부**만 보여 줍니다.
DPS 숫자는 화면에 내지 않습니다 (내부 계산용).

### 데이터

pvpoke 는 PvP 전용 수치만 있어 레이드 계산에 못 씁니다 (카운터: PvP 위력 8/2턴, 레이드 위력 13/1000ms).
그래서 **PokeMiners GAME_MASTER** 에서 PvE 기술 수치·타입 상성·전투 상수만 뽑아 `pve.json` 으로 둡니다.

- 만드는 법: `node tools/build-pve.mjs` (원본 19.5MB → 24KB, gzip 5KB)
- 갱신: `.github/workflows/pve.yml` 이 **매일 04:20 KST 에 자동으로** 다시 만들어 커밋합니다.
- 종족값·타입·기술풀·대기머·출시여부는 pvpoke 것을 그대로 씁니다. 둘의 종족값은 1723종이 일치합니다.
- 상성표의 `attackScalar` 인덱스는 알파벳순이 **아니라** 게임 고정 순서입니다. 빌드 스크립트가 알려진 상성 9개로 검산하고, 어긋나면 빌드를 멈춥니다.

### 메가 전용 3번째 기술

메가레벨 4 에서 5000 메가에너지로 여는 기술이 **13개** 있습니다 (11종, 라이츄·뮤츠는 X/Y 각각).
GAME_MASTER 의 `VM_MOVE_TEMP_EVOLUTION_MEGA[_X|_Y]_V####_POKEMON_XXX` 템플릿에 들어 있고,
**pvpoke 기술풀에는 없어서** 챙기지 않으면 이 13마리가 통째로 과소평가됩니다.

- `pve.json` 의 `megaMoves` 에 `"도감번호_폼접미사"`(예: `149_mega`, `26_mega_x`) 키로 담깁니다.
- 이름은 템플릿의 `vfxName` 으로 원본 기술을 찾아 그 한글명을 씁니다. 원본을 못 찾으면 빌드가 멈춥니다.
- 같은 이름의 일반 기술과 **다른 기술**입니다 (메가 망나뇽 역린: 전용 185위력 / 일반 110위력).
- 1위 조합이 전용기면 **둘째 줄은 전용기를 안 쓴 조합**을 보여 줍니다. 5000 에너지를 쓸 값어치를 견주기 위해서입니다.

메가레벨 자체의 효과는 레벨 1~4 가 같습니다 (팀 공격 보너스 자속 1.3 / 비자속 1.1).
레벨 4 만 `selfCpBoostAdditionalLevel: 2` 로 자기 CP 를 2레벨 올려 주는데, 공격 기준 약 +1.3% 라
순위에 영향이 없어 계산에 넣지 않았습니다.

### 순위 계산

- 타입 T 순위는 **T 에 약한 보스(×1.6)를 때린다**는 전제로 냅니다. 이걸 빼면 뮤츠가 염동력+냉동빔으로 얼음 1위가 되는 엉뚱한 결과가 나옵니다.
- 점수는 **DPS³ × TDO** (= DPS⁴ × 체력 × 방어). DPS 만 쓰면 물몸 딜러가 과대평가됩니다.
- **그림자는 메가진화를 못 합니다.** `canShadow()` 에서 메가·원시를 제외합니다. 빠뜨리면 "그림자 메가 뮤츠" 가 상위권을 도배합니다.
- 회피·상대 타입 조합·**피격 시 차는 에너지**는 넣지 않은 단순 모형입니다. 그래서 록온처럼 위력이 낮고 에너지가 빨리 차는 일반기를 다소 후하게 봅니다.
- 그래서 기술배치를 **하나만 내지 않고 좋은 순으로 두 개**를 냅니다. 계산이 고른 것과 실제로 흔히 쓰는 것이 갈릴 때 사용자가 직접 판단할 수 있게 하기 위해서입니다.
- DPS·TDO 는 **그 타입 1위를 100** 으로 놓은 상대값입니다 (한 마리 탭의 "최적 대비 %" 와 같은 방식).

### 포케토리 표와의 대조 (2026-09-07, 바위)

포케토리 티어표(https://poketory.com/pokemongo_tier)의 DPS%/TDO% 와 대조했습니다.
제 값이 전반적으로 6~8% 높지만 **종마다 다른 게 아니라 일정하게 밀린 계통 오차**라 순위에는 영향이 없습니다.
그림자와 일반형의 TDO 가 같게 나오는 것도 양쪽이 일치합니다 (TDO ∝ DPS × 체력 × 방어).

| 대상 | 내 DPS% / 포케토리 | 내 TDO% / 포케토리 |
|---|---|---|
| 그림자 램펄드 | 134 / 138 | 59 / 60 |
| 램펄드 | 113 / 113 | 60 / 60 |
| 그림자 거대코뿌리 | 122 / 120 | 102 / 100 |
| 메가 마기라스 | 115 / 108 | 147 / 139 |
| 메가 디안시 | 131 / 124 | 91 / 87 |
| 그림자 견고라스 | 127 / 118 | 83 / 77 |
| 거대코뿌리(기준) | 100 / 100 | 100 / 100 |

주의: 포케토리의 "유리대포" 분류는 표의 DPS/TDO 숫자로 설명되지 않습니다
(그림자 견고라스 TDO 77 < 그림자 기가이어스 80 인데 견고라스가 S+). 편집자 판단으로 보이며, 이 앱은 따르지 않습니다.

## 판정 기준 (사용자 지정)

1. 메타 순위(기본 200위) 안에 드는 자리가 없으면 → 정리
2. 있어도 최적 IV 대비 기준치(기본 99%) 미만이면 → 정리
3. 단, 이벤트 특전기 습득 개체는 예외로 유지
4. 판정은 **진화 계통 전체**를 계산해서 가장 좋은 자리 기준으로 내립니다
